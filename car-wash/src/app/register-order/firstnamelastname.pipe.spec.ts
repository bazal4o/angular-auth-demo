import { FirstnamelastnamePipe } from './firstnamelastname.pipe';

describe('FirstnamelastnamePipe', () => {
  it('create an instance', () => {
    const pipe = new FirstnamelastnamePipe();
    expect(pipe).toBeTruthy();
  });
});
